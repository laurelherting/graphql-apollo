schema {
  query: RootQuery
  mutation: Mutations
}

type Mutations {
  changeUserName(newName: String, userId: ID): User
}

type RootQuery {
  users: [User]
  myuser(id: ID, name: String): Viewer
}

type User {
  id: ID
}

type Viewer {
  user: User
}
