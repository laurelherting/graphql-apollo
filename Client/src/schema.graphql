schema {
  query: RootQuery
  mutation: Mutation
}

type Mutation {
  changeBookName(newName: String!, bookId: ID!): Book
}

type RootQuery {
  books: [Book]
  mybook(bookId: ID!, name: String): Viewer
}

type Book {
  id: ID
  name: String
}

type Viewer {
  book: Book
}
